package teluri.mods.jlrays.config;

public interface ISamplesHandler {

	public static enum SamplesHandlers implements ISamplesHandler {
		ONE_PER_VOLUME, ONE_PER_FACE // later, TWO_PER_FACE // and maybe FOUR_PER_FACE
	}
}
